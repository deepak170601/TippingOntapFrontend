<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- NFC — required for Tap to Pay -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="true" />

    <!-- Bluetooth — required by Stripe Terminal -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- Location — required by Stripe Terminal -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Event reminders declare no permissions here on purpose. Notifee's core
         AAR already merges in POST_NOTIFICATIONS, RECEIVE_BOOT_COMPLETED (with
         a RebootBroadcastReceiver, so reminders survive a restart), WAKE_LOCK
         and VIBRATE. Repeating them here would only make it look as though
         removing one from this file would change anything.

         Note for the Play listing: that same AAR also merges in
         SCHEDULE_EXACT_ALARM, which we do not use — src/services/notifications.ts
         schedules inexact alarms deliberately. If Play questions it, it can be
         stripped with tools:node="remove" without affecting reminders. -->

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true"
      android:exported="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">

        <!-- App launcher -->
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        <!-- Deep link — Stripe Connect onboarding return -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="tippingonthego" android:host="connect" />
        </intent-filter>

      </activity>
    </application>

</manifest>